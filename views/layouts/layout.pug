doctype html
html
    head
        title #{title}
        link(rel="stylesheet", href="/styles.css")
        script(src="https://unpkg.com/htmx.org@2.0.4")
    body(hx-boost="true" hx-indicator=".loader")
        header
            nav
                img(src="/shuriken.png")
                h1 The Dojo Chronicles
        div(hx-history-elt)
            block content
        include ../partials/loader